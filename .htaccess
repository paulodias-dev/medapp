RewriteEngine On

# Serve arquivos/pastas existentes
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA: qualquer rota vai pro index.html
RewriteRule ^ index.html [L]
